# QuestLife Shared

Shared code can live here when the App and Admin projects need the same types, schemas, constants, or helper functions.

Keep secrets out of this folder. Both frontends should only use public Supabase environment variables.
